/// <reference path="../../adonis-typings/mail.d.ts" />
import { MessageNode, FakeDriverContract, FakeMailResponse, MessageSearchNode } from '@ioc:Adonis/Addons/Mail';
/**
 * Smtp driver to send email using smtp
 */
export declare class FakeDriver implements FakeDriverContract {
    private transporter;
    mails: MessageNode[];
    constructor();
    /**
     * Find an email
     */
    find(messageOrCallback: MessageSearchNode | ((mail: MessageSearchNode) => boolean)): MessageNode | null;
    /**
     * Filter emails
     */
    filter(messageOrCallback: MessageSearchNode | ((mail: MessageSearchNode) => boolean)): MessageNode[];
    /**
     * Send message
     */
    send(message: MessageNode): Promise<FakeMailResponse>;
    /**
     * Close transporter connection, helpful when using connections pool
     */
    close(): Promise<void>;
}
