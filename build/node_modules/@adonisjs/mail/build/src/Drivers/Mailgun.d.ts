/// <reference path="../../adonis-typings/mail.d.ts" />
/// <reference types="@adonisjs/logger/build/adonis-typings/logger" />
import { LoggerContract } from '@ioc:Adonis/Core/Logger';
import { MessageNode, MailgunConfig, MailgunResponse, MailgunRuntimeConfig, MailgunDriverContract } from '@ioc:Adonis/Addons/Mail';
/**
 * Ses driver to send email using ses
 */
export declare class MailgunDriver implements MailgunDriverContract {
    private config;
    private logger;
    constructor(config: MailgunConfig, logger: LoggerContract);
    /**
     * Send message
     */
    send(message: MessageNode, config?: MailgunRuntimeConfig): Promise<MailgunResponse>;
    close(): Promise<void>;
}
