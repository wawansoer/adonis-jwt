"use strict";
/*
 * @adonisjs/mail
 *
 * (c) Harminder Virk <virk@adonisjs.com>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MailgunDriver = void 0;
/// <reference path="../../adonis-typings/mail.ts" />
const nodemailer_1 = __importDefault(require("nodemailer"));
const Mailgun_1 = require("../Transports/Mailgun");
/**
 * Ses driver to send email using ses
 */
class MailgunDriver {
    constructor(config, logger) {
        this.config = config;
        this.logger = logger;
    }
    /**
     * Send message
     */
    async send(message, config) {
        const transporter = nodemailer_1.default.createTransport(new Mailgun_1.MailgunTransport({
            ...this.config,
            ...config,
        }, this.logger));
        return transporter.sendMail(message);
    }
    async close() { }
}
exports.MailgunDriver = MailgunDriver;
