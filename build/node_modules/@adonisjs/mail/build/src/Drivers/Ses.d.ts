/// <reference path="../../adonis-typings/mail.d.ts" />
import * as aws from 'aws-sdk';
import { MessageNode, SesMailResponse, SesConfig, SesDriverContract } from '@ioc:Adonis/Addons/Mail';
/**
 * Ses driver to send email using ses
 */
export declare class SesDriver implements SesDriverContract {
    private transporter;
    constructor(config: SesConfig);
    /**
     * Send message
     */
    send(message: MessageNode, options?: Omit<aws.SES.Types.SendRawEmailRequest, 'RawMessage' | 'Source' | 'Destinations'>): Promise<SesMailResponse>;
    /**
     * Close transporter connection, helpful when using connections pool
     */
    close(): Promise<void>;
}
