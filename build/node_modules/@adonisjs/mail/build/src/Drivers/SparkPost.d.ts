/// <reference path="../../adonis-typings/mail.d.ts" />
/// <reference types="@adonisjs/logger/build/adonis-typings/logger" />
import { LoggerContract } from '@ioc:Adonis/Core/Logger';
import { MessageNode, SparkPostConfig, SparkPostResponse, SparkPostRuntimeConfig, SparkPostDriverContract } from '@ioc:Adonis/Addons/Mail';
/**
 * Ses driver to send email using ses
 */
export declare class SparkPostDriver implements SparkPostDriverContract {
    private config;
    private logger;
    constructor(config: SparkPostConfig, logger: LoggerContract);
    /**
     * Send message
     */
    send(message: MessageNode, config?: SparkPostRuntimeConfig): Promise<SparkPostResponse>;
    close(): Promise<void>;
}
