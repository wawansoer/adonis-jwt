/// <reference path="../../adonis-typings/mail.d.ts" />
import { MailerContract, MailersList, MessageNode, MessageSearchNode, FakeMailManagerContract } from '@ioc:Adonis/Addons/Mail';
export declare class FakeMailManager implements FakeMailManagerContract {
    fakedMailers: Map<keyof MailersList, MailerContract<any>>;
    /**
     * Returns the faked mailer instance
     */
    use(mailer: keyof MailersList): MailerContract<any>;
    /**
     * Restore mailer fake
     */
    restore(mailer: keyof MailersList): void;
    /**
     * Find if a mailer is faked
     */
    isFaked(mailer: keyof MailersList): boolean;
    /**
     * Find if an email exists
     */
    exists(messageOrCallback: MessageSearchNode | ((mail: MessageSearchNode) => boolean)): boolean;
    /**
     * Find an email
     */
    find(messageOrCallback: MessageSearchNode | ((mail: MessageSearchNode) => boolean)): MessageSearchNode | null;
    /**
     * Filter emails
     */
    filter(messageOrCallback: MessageSearchNode | ((mail: MessageSearchNode) => boolean)): MessageNode[];
}
